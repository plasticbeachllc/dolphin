# Copy this file to .env and fill in values before running docker-compose.
# Required variables have no default — docker-compose will refuse to start without them.

# Grafana admin credentials (required, no default)
GRAFANA_ADMIN_USER=
GRAFANA_ADMIN_PASSWORD=
